# Rainbowave-CLI

Rainbowave CLI is a compiled binary CLI utility which creates a beautiful ASCII wave pattern across the terminal window with a constant rainbow gradient flowing through it.

Rainbowave CLI takes advantage of GPU accelerated terminal emulators to produce prettier and smoother visuals.
